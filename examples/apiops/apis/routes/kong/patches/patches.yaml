_format_version: "1.0"

patches:
  - selectors:
    - $.services[?(@.name=="routes-service")]
    values:
      host: flight-data-routes-api.flight-data.svc.cluster.local
      port: 8082
      protocol: http
