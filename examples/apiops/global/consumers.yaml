_format_version: '3.0'

consumers:
  - username: anonymous
    plugins:
      - name: request-termination
        config:
          status_code: 403
          message: "You cannot consume this service"
          content_type: "application/json"