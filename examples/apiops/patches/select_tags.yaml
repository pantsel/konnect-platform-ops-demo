---
_format_version: '3.0'
patches:
- selectors:
  - "$"
  values:
    _info:
      defaults: {}
      select_tags:
      - ${{ env "DECK_API_NAME" }}
      - ${{ env "DECK_CONFIG_OWNER" }}
      - ${{ env "DECK_ORGANIZATION" }}