_format_version: '3.0'
_info:
  defaults: {}
  select_tags:
  - observability
  - platform_team

plugins:
  - name: prometheus
    config:
      per_consumer: true
      status_code_metrics: true
      latency_metrics: true
      bandwidth_metrics: true

  - name: opentelemetry
    config:
      traces_endpoint: ${{ env "DECK_CONFIG_OPENTELEMETRY_ENDPOINT" }}
      resource_attributes:
        namespace: kong
        service.name: kong-dp
      send_timeout: 5000
