---
_format_version: '3.0'
add-plugins:
- selectors:
  - "$"
  overwrite: false
  plugins:
  - name: datadog
    enabled: false
    config:
      host: datadog-agent.${{ env "DECK_CONFIG_NAMESPACE"}}.svc.cluster.local
      port: 8125
      flush_timeout: 2
      retry_count: 10